@{
    var addMemberUrl = Url.Action("AddMemberPage", "Admin", new { area = "Admin" });
    var addLoanUrl = Url.Action("AddLoanPage", "Admin", new { area = "Admin" });
}
<div class="dropdown">
    <button class="dropdown-btn">ADMINISTRATIVE</button>
    <div class="dropdown-content">
        <a href="@addMemberUrl" class="nav-button">Add a Member</a>
        <a href="@addLoanUrl" class="nav-button">Loan Application</a>
        <a href="#">File Login Details</a>
        <a href="#">Payment</a>
    </div>
</div>
